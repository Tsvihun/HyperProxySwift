# Quick Start

Choose the smallest provider product your app needs, construct its service with a HyperProxy
gateway URL and app key, then send a compile-time operation with a provider-native payload.

The SDK never inserts a proprietary provider payload wrapper.

For an executable example, see `Examples/QuickStart`.
